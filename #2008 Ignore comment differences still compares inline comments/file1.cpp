
   if ((WindowWidth >= Buffer->Width * 2) && // If display can support a 2x scale full screen
       (WindowHeight >= Buffer->Height * 2))
   {
       // TODO: Centering with blanck bars for dispays above 2X scale?
   }
   else
   {
   }

